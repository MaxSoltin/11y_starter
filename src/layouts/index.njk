{% include "header.njk" %}
	<main class="mainContent">
		<div class="container">
			<div class="row">
				<h1>{{ title }}</h1>
				<div>
					{% for post in collections.posts %}
						<li>
							<a href="{{post.url}}">{{post.data.title}}</a>
						</li>
					{% endfor %}
				</div>
		</div>
	</main>
{% include "footer.njk" %}
